package org.jgum.graph;

/**
 * Determines if a path of nodes (i.e., an iterable) should filter nodes that have already been visited
 * @author sergioc
 *
 */
public enum DuplicatesDetection {
	ENFORCE, IGNORE; 
}
